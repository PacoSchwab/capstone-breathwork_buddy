/* import Counter from "../Counter";
import * as V from "victory";
import useLocalStorage from "use-local-storage";

export default function TrackingSection() {
  const storedTimes = useLocalStorage("storedTimes", []);
  return;
  <p>{storedTimes}</p>;
}
 */
