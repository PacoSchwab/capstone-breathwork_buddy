import styled from "styled-components";
import { StyledSlideAnimation } from "./StyledSlideAnimation";

export const StyledTrackingContainer = styled.div`
  animation: ${StyledSlideAnimation} 1s ease 0s 1 normal forwards;
  padding: 1rem;
`;
